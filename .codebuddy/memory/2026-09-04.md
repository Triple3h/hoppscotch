# 2026-09-04

## 配置 GitHub Actions 桌面端打包（版本定为 1.0.0）
- 删除上游遗留 `.github/workflows/build-hoppscotch-desktop.yml`（指向官方仓库/不可用签名 secrets）。
- 新增 `.github/workflows/release-desktop.yml`：workflow_dispatch（默认 version=1.0.0，create_release=true）+ push v* tag 触发。流水线：resolve-version → bundle-web（ubuntu 上一次构建 selfhost-web 前端 + 跑 webapp-bundler 产出 bundle.zip/manifest.json）→ build-linux(deb,appimage)/build-windows(nsis,msi)/build-macos(matrix x86_64+aarch64, dmg+app.zip) → release（gh release create v<version> 上传安装包）。
- 新增 `.github/scripts/set-desktop-version.mjs`：构建前统一把 desktop 版本写入 package.json / tauri.conf.json / portable confs / Cargo.toml / Cargo.lock（CI 与 tag 版本保持一致）。
- 桌面版本 26.8.0 → 1.0.0（上述文件全部更新，含 Cargo.lock 根 crate）。
- `tauri.conf.json` 的 `createUpdaterArtifacts` true→false：fork 无更新服务器/签名密钥，否则 `tauri build` 会因缺 `TAURI_SIGNING_PRIVATE_KEY` 失败（portable confs 本来就是 false）。
- 注意点：安装包均未签名（macOS/Windows 首装会有安全提示）；桌面 release 产物命名 `hoppscotch-desktop_<ver>_<platform>_<arch>.*`。
- 已提交 `6fb7559ce`（feat: set up desktop release CI and bump desktop to v1.0.0）并推送 main；创建并推送 annotated tag `v1.0.0`，已触发 release-desktop.yml 的 push tag 构建（含之前遗留的 components.d.ts 一并提交）。
- CI 首次失败修复：`gql-gen/backend-schema.gql` 被根 `.gitignore`（`gql-gen/`）忽略、从未入库，CI 中 common 的 postinstall gql-codegen 找不到 schema 而失败（AGENTS.md "already committed" 与实际不符）。已 `git add -f` 提交（`e1a95ef0c`），并把 `v1.0.0` 标签删旧重打至该提交重新触发构建。教训：该 fork 的 schema 文件必须显式入库。

## 桌面自动更新切到本仓库 GitHub Releases（未提交，等用户确认）
- 背景：用户问本地版为何会弹上游自动更新（1.0.0 查 `releases.hoppscotch.com`），随后要求实现自己的自动更新。
- 生成本 fork 专属 minisign 密钥对：私钥 `~/.tauri/hoppscotch-desktop.key`（口令在同目录 `hoppscotch-desktop.key.passphrase`），公钥已写入三个 tauri conf 的 `pubkey`。私钥不入库，需用户配置为 GitHub secrets `TAURI_SIGNING_PRIVATE_KEY` / `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`。
- 三个 tauri conf（tauri.conf.json + portable macos/windows）endpoints 改为 `https://github.com/Triple3h/hoppscotch/releases/latest/download/hoppscotch-desktop-latest.json`；`createUpdaterArtifacts` 仍为 false（避免本地无 key 构建失败），CI 用 `--config '{"bundle":{"createUpdaterArtifacts":true}}'` 开启并注入签名 secrets。
- `src-tauri/src/updater.rs` portable 模式下载 URL 改为本仓库 releases 页（原硬编码 hoppscotch.com/download）。
- `.github/workflows/release-desktop.yml`：平台构建注入签名 env + 收集 `.sig` / mac `.app.tar.gz` 产物；release job 新增 Build update manifest step（用新脚本生成并上传 manifest）。
- 新增 `.github/scripts/build-update-manifest.mjs`：扫描 release-assets 里规范化命名（`hoppscotch-desktop_<ver>_<platform>...`）生成 Tauri v2 manifest JSON（linux/windows/darwin x86_64+aarch64），signature 取 `<asset>.sig`。已在本地用假文件自测通过。
- 文档同步：SECURITY.md（两处 releases.hoppscotch.com 描述）、desktop README 下载链接。
- 残留未处理：`common/src/composables/whats-new.ts` fetch `releases.hoppscotch.com/releases/${v}.json`、`request.inspector.ts` 的 learn_more 链接仍指官网 —— 属 web 侧另一功能，未改。
- 待办：用户配好 secrets 后提交（feat: switch auto-update to fork GitHub Releases）；新发版需比已装版本高（首个启用更新的版本建议 v1.0.1+）。macOS 未 Developer ID 签名，自动更新替换后可能被 Gatekeeper 拦，Windows 未签名会有 SmartScreen 提示，均属已知限制。
