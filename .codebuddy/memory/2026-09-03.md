# 2026-09-03

## 响应区新增“实际请求”Tab（参考 Apifox）
- 新组件 `packages/hoppscotch-common/src/components/lenses/ActualRequestRenderer.vue`：展示实际发送的请求（方法徽标+最终 URL、最终 Headers、Body、请求代码生成器）。
- 修改 `ResponseBodyRenderer.vue`：新增 `actual-request` Tab + `actualRequest` computed（`"req" in response` 判断，除 script_fail 外所有响应都带 req）。
- 数据来源：`response.req` 是 `EffectiveHoppRESTRequest`（`network.ts` 存入 cloneDeep 的 effective 请求），含 `effectiveFinalURL/effectiveFinalHeaders/effectiveFinalBody/effectiveFinalParams`。
- 请求代码复用 `helpers/new-codegen` 的 `generateCode`，用 `makeRESTRequest` 把 effective 字段映射回 HoppRESTRequest。
- i18n：`response.actual_request / request_url / request_body / request_code` 加入 en.json + cn.json。
- 验证：`node type-check.mjs`（common 包 vue-tsc CLI 会崩，勿直接用）+ eslint 通过。
